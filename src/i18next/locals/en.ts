// Page Translation
import translateTopNavigation from '../translations/en/topNavigation';
import translateHero from '../translations/en/translateHero';
import translateGallery from '../translations/en/translateGallery';

const en = {
  translateTopNavigation,
  translateHero,
  translateGallery,
};

export default en;
