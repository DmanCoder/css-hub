import { GalleryTypes, MediaTypes } from '../../typescriptGlobals/types';

export interface INotFoundProps {}

export type UseNotFoundReturnTypes = {
  myList: MediaTypes[];
  tabs: GalleryTypes[];
};
